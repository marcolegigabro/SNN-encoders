# Task 1: end-to-end learned spike-train compression with channel

> Implement in Python an end-to-end communication system in which an SNN-based
> encoder compresses an input spike train, the compressed representation is
> transmitted over a communication channel, and an SNN-based decoder
> reconstructs the original spike train. Use different spike sources (e.g.
> rate coding, temporal coding, time-to-first-spike coding) and different
> channel models, in particular models inspired by satellite communications as
> well as timing-channel models.

Unclaimed. Nothing here yet.
